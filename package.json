{
  "name": "aurorium-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp -R public/. dist/",
    "lint": "node --check scripts/validate-html.mjs"
  }
}
